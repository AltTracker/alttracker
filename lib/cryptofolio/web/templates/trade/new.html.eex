<h2>New Trade</h2>

<%= render "form.html", changeset: @changeset,
                        currencies: @currencies,
                        btc_price: @btc_price,
                        action: portfolio_trade_path(@conn, :create, @portfolio.id) %>

<span><%= link "Back", to: portfolio_path(@conn, :show, @portfolio.id) %></span>
